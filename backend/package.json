{
  "name": "grant-gni-backend",
  "version": "0.1.0",
  "description": "Grant Gni backend gateway. Proxies LLM calls (Gemini now, Vertex AI later) so the Word/Excel add-in never holds provider keys. Auth + subscriptions land in v2.",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
